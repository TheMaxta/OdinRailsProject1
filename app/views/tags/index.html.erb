<h1>All Tags</h1>
	<ul id="articles">
		<% @tags.each do |tag| %>
			<li>
			<%= link_to tag.name, tag_path(tag) %>
			<h7 class="tag_buttons"><%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Really delete the article?"} %></h7>
			</li>
<% end %>	